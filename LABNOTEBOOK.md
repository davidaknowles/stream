# Lab Notebook

## 2026-08-06

- Extended only the raw gene-scaled population fine-tunes from a 200- to 400-update maximum, retaining validation every 20 updates, five-check patience, and the 0.2% minimum relative improvement rule. The existing selected checkpoints do not persist the final training/RNG state, so exact continuation is unavailable; the extensions are deterministic fresh runs from the same score-flow parents under new labels, preserving the original artifacts. FiLM training/evaluation jobs are `19594173`/`19594174`, and cross-attention jobs are `19594175`/`19594176`.

## 2026-08-05

- The first growth-only/joint smokes `19533138`/`19533139` initialized and saved valid zero-growth checkpoints, then failed after update one because gradients through 80 Sinkhorn matrix-scaling iterations made the growth head non-finite. Replaced that path with detached Sinkhorn plans and exact envelope gradients from source/target dual potentials. The analytic gradient matches central finite differences within `3.5e-6`; gradient clipping now raises before any non-finite optimizer update, and all 57 tests pass.
- Cancelled only the dependency-dead descendants and submitted corrected smoke/evaluation chains `19536239`/`19536241` and `19536240`/`19536242`. Corrected production jobs are FiLM growth-only/joint `19536243`/`19536245` and cross-attention growth-only/joint `19536247`/`19536249`, with each following even job as its causal evaluation; summary job `19536251` depends on all four.
- The v2 smokes stopped safely before the optimizer because the zero-initialized rate RMS diagnostic had a singular square-root derivative when its square was reused as the regularizer. Growth regularization now uses mean squared rate directly and reports a detached RMS. Added an end-to-end zero-growth gradient regression; all 58 tests pass. Cancelled the second dependency-dead descendants and submitted v3 smoke/evaluation chains `19543003`/`19543005` and `19543004`/`19543006`. Production growth-only/joint jobs are FiLM `19543007`/`19543009` and cross-attention `19543011`/`19543013`, each followed by its evaluation; summary is `19543015`.
- Both v3 training smokes completed one finite update and validation. Growth-only had gradient norm `0.0415`; joint had finite pre-clipping norm `2.62`. Effective sample size remained exactly the full 32 and 2 cells after one tiny update, respectively, so there was no weight collapse. The first growth-only causal reload exhausted 96 GB because evaluation retained differentiable 10,000-gene rollout graphs across controls. Growth evaluation now runs under `torch.no_grad`, and redundant duplicate growth-only rollouts were removed; replacement reload job is `19545630`. Production FiLM and cross-attention growth-only jobs `19543007` and `19543011` have started, while joint jobs remain queued.
- All four raw-endpoint growth fits completed. Growth-only improved validation too little to meet the 0.2% checkpoint threshold, so both architectures retained update zero with exactly uniform weights. Joint growth selected update 180 for FiLM and 200 for cross-attention, but validation EMA improved only from `1.06239` to `1.06146` versus matched no-growth FiLM and from `1.06388` to `1.06131` for cross-attention. Effective sample sizes remained `15.988/16` and `15.981/16`, indicating negligible abundance reweighting. All four production causal evaluations are running; held-out results are required before concluding whether the small joint gain generalizes.
- All four growth causal evaluations and the summary job completed. Growth-only checkpoints remained at update zero, and their growth-only rollout reproduced persistence to numerical precision (mean Sinkhorn skill `1.8e-9`). Within jointly trained checkpoints, learned growth increased mean deterministic Sinkhorn skill by only `0.00057` for FiLM and `0.00100` for cross-attention, while worsening mean-shift R-squared by `0.100` and `0.118`, respectively, and slightly increasing mean-shift MAE.
- Joint-rollout weights remained close to uniform: deterministic effective sample size averaged `127.70/128` for FiLM and `127.48/128` for cross-attention. Growth-only controls were also ineffective, with Sinkhorn skill near zero and mean-shift R-squared of `-0.135` and `-0.160`. Learned-score and zero-score SDE controls showed the same growth tradeoff, so the result is not score-dependent.
- Compared with separately fitted no-growth population models, joint growth did not improve FiLM (`0.09398` versus `0.09426` Sinkhorn skill; `-0.615` versus `-0.469` R-squared). Cross-attention gained `0.00105` Sinkhorn skill (`0.09139` versus `0.09034`) but worsened R-squared from `-0.110` to `-0.225`. The approximately `0.09` endpoint skill therefore comes from population fine-tuning of the velocity model, not useful abundance modeling. Corrected the summary script to keep growth-only, joint, population, and base score-flow checkpoint families separate.

## 2026-08-04

- Implemented UCE-conditioned relative growth for population rollouts. A zero-initialized scalar head predicts bounded cell-specific rates; weighted particles are updated by centered exponential growth and renormalized because snapshot stages identify relative lineage abundance but not absolute embryo size. Weighted Sinkhorn, gene means, and PCA covariance propagate gradients to growth, with rate and weight-KL regularization and effective-sample-size diagnostics. Growth-only bypasses the 10,000-gene velocity field; joint mode retains coupled ODE/SDE dynamics. Causal evaluation includes fixed-mass velocity, growth-only, and joint growth controls while preserving learned-score versus zero-score comparisons. All 56 focused and regression tests pass.
- Submitted GPU-gated growth validation. Growth-only and joint one-update smokes are `19533138` and `19533139`, with causal reload/evaluation jobs `19533163` and `19533164`. Dependent raw-endpoint production comparisons are FiLM growth-only/joint `19533177`/`19533179` and cross-attention growth-only/joint `19533181`/`19533183`, with each following even job as its causal evaluation; summary job `19533185` combines the four evaluations. Growth-only uses 64 source and 128 target cells because it bypasses the gene-velocity field, while joint fitting retains 8 sources with two particles and 32 targets.
- Implemented fixed per-gene dynamical coordinates `Y = X / q`, where `q` is estimated from training stages only with a robust positive floor. OT remains in expression/PCA space; interpolation, velocity/score targets, losses, and ODE/SDE integration use `Y`; current states are mapped deterministically back to nonnegative expression `X = qY` before online UCE and before all endpoint metrics. There is no centering, log transform, stochastic count reconstruction, or use of `q` as a learned conditioning input.
- Applied the same coordinate contract to deterministic CFM, coupled flow/score matching, explicit population-rollout fine-tuning, and latent-clock proposals. Legacy count-coordinate checkpoints remain loadable, while new checkpoints persist the coordinate mode and 10,000-gene scale vector under explicit v2/v3/v4 contracts. The manuscript, SDE proposal, and pseudotime proposal now distinguish expression boundaries from internal dynamical coordinates.
- Added exact round-trip, interpolation, velocity, noise, Euler-rollout, sampler-calibration, checkpoint-compatibility, and paired-metric tests. All 52 unit tests pass. Gene-scaled deterministic CFM smoke `19529539` and coupled score-flow smoke `19529538` completed on GPU; population-rollout smoke `19529738` completed and wrote a reloadable gene-scaled v4 checkpoint. Tiny held-out rollout jobs are interface checks, not efficacy estimates.
- Submitted the matched gene-scaled FiLM/cross-attention by raw/KNN/metacell score-flow matrix. Training/evaluation jobs are `19529876`/`19529877`, `19529878`/`19529879`, `19529880`/`19529881`, `19529882`/`19529883`, `19529884`/`19529885`, and `19529886`/`19529887`; summary job `19529888` depends on all evaluations. Their explicit population-rollout fine-tunes/evaluations are `19529905`/`19529906`, `19529907`/`19529908`, `19529909`/`19529910`, `19529911`/`19529912`, `19529913`/`19529914`, and `19529915`/`19529916`.
- Added `docs/pseudotime.tex`, a standalone proposal for learning pseudotime inside STREAM as a stochastic developmental clock. A positive UCE-conditioned scalar increment controls coherent movement along a normalized CRE-conditioned gene-velocity direction, while optional orthogonal diffusion represents non-clock variation. The proposal covers identifiability, unpaired population fitting, proliferation and branching limitations, matched deterministic/stochastic ablations, clock-specific diagnostics, and implementation steps. It also defines scalar clock-aligned flow/score matching for simulation-free pretraining, explains its endpoint-coupling and Gaussian-clock limitations, and recommends unpaired positive-clock rollout fine-tuning. Added a dimensions table and defined `q` as the positive vector of per-gene expression scales; clock noise `z` is scalar, whereas regular score-flow noise is a G-vector. The nine-page PDF compiles without TeX warnings or unresolved references.
- Added population-level fine-tuning of coupled score-flow checkpoints through explicit Euler--Maruyama rollouts. Source and target cells are sampled independently within each observed interval, eliminating OT cell-pair couplings from this stage; gene-scaled checkpoints now integrate in `Y` and map endpoints back to expression for the population loss.
- Each update averages a coupled ODE endpoint loss and a positive-diffusion SDE endpoint loss. The loss combines debiased Sinkhorn divergence in whitened train-only PCA coordinates, robustly scaled gene-mean error, PCA covariance error, and a pretrained-parameter anchor.
- Fine-tuning freezes STREAM's regulatory trunk and autonomous velocity control, and updates only the path-coordinate modulation, conditional velocity, and shared noise heads. Raw current expression is re-encoded by frozen UCE at each step; the discrete encoder is stop-gradient and no KNN/metacell smoothing is applied during rollout.
- Added fixed, disjoint validation populations and Brownian seeds, EMA checkpoint selection including the pretrained update-zero baseline, a reusable population rollout/loss module, evaluation support for the new checkpoint contract, and focused gradient and parameter-freezing tests. All 47 unit tests pass.
- GPU smoke job `19510887` completed a full baseline-validation, ODE/SDE rollout, backward update, post-update validation, and v3 checkpoint reload. The rollout gradient norm was 0.375 and all losses were finite. The one-step validation change was below the 0.2% selection threshold, so the pretrained update-zero checkpoint was correctly retained.
- Held-out evaluation smoke job `19512108` loaded the v3 checkpoint and completed all autonomous, coupled, learned-score, and zero-score controls across the three middle-block forecast horizons. This was a two-cell, one-step interface test rather than an efficacy estimate.
- Submitted population fine-tuning and dependent held-out middle-block evaluation for all six FiLM/cross-attention by raw/KNN/metacell pretrained models. Train/evaluation jobs are `19512056`/`19512057`, `19512058`/`19512059`, `19512060`/`19512061`, `19512062`/`19512063`, `19512064`/`19512065`, and `19512066`/`19512067`. The two jobs whose parent score-flow fits are still active also depend on those parent jobs.
- Four population fine-tunes completed without errors. From pretrained baseline to final validation check, endpoint objective improved by 3.92% for raw FiLM, 2.95% for KNN FiLM, 3.54% for KNN cross-attention, and 3.41% for metacell cross-attention. Checkpoint selection chose update 160 for both FiLM runs and update 200 for both cross-attention runs. Their observed-stage ODE and SDE Sinkhorn skills are approximately 6%, but held-out middle-block evaluations remain in progress or queued and are required before interpreting this as improved generalization.

## 2026-08-03

- Replaced the weak independent score/flow heads with an analytically coupled stochastic-interpolant parameterization. One shared $\tau$-modulated promoter representation predicts conditional endpoint velocity and standardized bridge noise. The coupled flow is the conditional velocity plus $\gamma'(\tau)\Sigma\widehat\epsilon/\Delta t$, and the score is $-\Sigma^{-1}\widehat\epsilon/\gamma(\tau)$, so both use the same noise estimate. A third readout from the unmodulated promoter features provides a strictly autonomous velocity control; neither path receives absolute developmental time.
- Extended causal evaluation to compare autonomous ODE, coupled probability-flow ODE, learned-score SDE, and a zero-score SDE with identical Brownian draws. The latter isolates learned score correction from generic variance broadening. The coupled loss normalizes by both endpoint-velocity RMS and analytic bridge-noise RMS. All 44 CPU tests pass, and GPU smoke jobs `19465662`/`19466070` completed the three-head checkpoint, all four rollout controls, and matched stochastic seeds without errors. Updated `docs/sde.tex` and its compiled PDF with the derivation, failed independent-head result, and controls.
- Submitted the corrected FiLM/cross-attention by raw/KNN/metacell matrix. Training jobs are `19466135`, `19466137`, `19466139`, `19466141`, `19466143`, and `19466145`; each even successor through `19466146` is its causal evaluation. Summary job `19466147` will write the coupled autonomous/learned-score/zero-score comparison after all evaluations finish.
- All six score-flow production runs, causal evaluations, and aggregate summary completed successfully. Every run stopped after 28 epochs and selected epoch 15. The validation score-noise MSE remained `0.9991`--`0.9997`, effectively the unit-variance null predictor; KNN and metacell denoising did not make technical noise predictable. Raw-endpoint normalized validation flow loss was about `1.09`, while KNN/metacell were worse at `1.26`--`1.37`, so neither head established useful observed-interval prediction.
- Deterministic score-flow ODE rollouts remained at persistence: mean Sinkhorn skill ranged from `-0.0016` to `0.0113`, and mean-shift R-squared was negative for every model. Adding diffusion produced consistent positive Sinkhorn skill across all three held-out horizons, peaking at `0.0787` for raw FiLM at diffusion `0.01`, but worsened mean-shift R-squared to `-0.428`. Because the score head stayed at its null loss, this distributional gain cannot be attributed to a learned score and is likely primarily variance broadening from injected noise. A matched diffusion-with-zero-score control is required before interpreting the stochastic gain.
- Replaced multinomial online-UCE token draws with one-offset systematic resampling and deterministic chromosome ordering. The frozen UCE model still receives one 1,024-gene sentence per cell, but the token histogram has much lower Monte Carlo variance without repeated UCE forward passes. On eight real interpolated 10k-gene atlas states with 12 repeats, mean embedding variance fell from `2.6e-5` to `1.3e-5`, mean cosine similarity to each cell's repeat mean rose from `0.9832` to `0.9916`, and throughput rose from 44.5 to 130.0 cell embeddings/s because sentence-length buckets were more consistent.
- Implemented simulation-free stochastic-interpolant STREAM with a shared CRE/UCE trunk, an autonomous per-gene count-velocity head, a noise-level-conditioned standardized denoising-score head, and fixed training-only gene scales. Training reuses the interval-stratified partial-OT/PCA-cost pair bank and supports raw, within-stage KNN-smoothed, or metacell-smoothed endpoints. Model selection uses observed-stage validation loss with EMA, a 0.5% improvement threshold, and 12-check patience.
- Added causal deterministic ODE and diagonal score-corrected Euler--Maruyama evaluation on the middle-three-stage holdout. KNN/metacell smoothing is used only to construct training targets; rollout recomputes systematic UCE from the current raw expression state. The implementation has 43 passing CPU tests. End-to-end GPU smoke jobs `19461360`/`19461361` completed training, checkpoint reload, ODE rollout, and stochastic rollout without errors. The smoke used only two training intervals and is not an efficacy estimate.
- The smoke identified an overly small velocity-scale floor for genes absent from its calibration sample; the implementation now applies the same `1e-3` second-moment floor convention as deterministic CFM. The pre-fix production jobs were cancelled before fitting. Corrected FiLM/cross-attention by raw/KNN/metacell training jobs are `19461388`, `19461390`, `19461392`, `19461394`, `19461396`, and `19461398`; each odd successor through `19461399` is its causal evaluation. Job `19461400` will combine the six evaluations into one persistence-relative Sinkhorn and gene-shift summary table.
- Added `docs/sde.tex`, a standalone proposal for stochastic STREAM dynamics. It retains gene-valued CRE-conditioned drift and now recommends simulation-free score-and-flow matching on noisy interpolants from an entropic endpoint coupling as the first experiment. The proposal derives analytic flow and denoising-score targets, the induced score-corrected SDE, its local interpolation-time requirement, and matched ODE controls. Population-level differentiable Sinkhorn training remains the autonomous comparison, alongside low-rank diffusion, Euler--Maruyama rollout, UCE gradient limitations, regularization, growth weighting, evaluation, implementation stages, and stopping criteria. The seven-page document compiles without TeX errors, undefined references, or overfull boxes.
- All six middle-three-stage models and causal evaluations completed without failure. Without a crossing bridge, mean 16-step Sinkhorn skill across `E13.25` forecasts to `E13.5`, `E13.75`, and `E14.0` was 0.0127/0.0151/0.0195 for adjacent/skip-1/skip-2, but mean-shift R-squared remained negative at -0.0442/-0.0621/-0.1068. Thus the slightly positive distributional score does not correspond to accurate gene-level shifts.
- Adding the observed `E13.25` to `E14.25` bridge changed mean skill to 0.0188/0.0066/0.0075 and mean-shift R-squared to -0.1493/-0.0579/-0.0355. Bridge+skip-2 substantially improved MAE, Wasserstein-1, and R-squared relative to non-bridge skip-2, including an `E14.0` R-squared of -0.0172, but reduced Sinkhorn skill and still did not beat persistence. Bridge training lowered selected normalized validation EMA for every setting, especially skip-2 from 1.1732 to 1.0355, while all remained above the normalized zero-velocity level.

## 2026-08-02

- Added matched middle-block bridge experiments. Each training set adds the observed-endpoint interval `E13.25` to `E14.25`, which crosses all three held-out stages without sampling their cells; PCA fitting and validation still exclude the block. The otherwise matched cross-attention jobs are adjacent-plus-bridge `19460159`, skip-1-plus-bridge `19460161`, and skip-2-plus-bridge `19460163`, with causal evaluations `19460160`, `19460162`, and `19460164`. This directly tests whether a long chord across the forecast gap supplies useful dynamics supervision or weakens the holdout.
- The complete 24-model large-bank causal matrix finished without failures. At 16 Euler steps, the best mean persistence-normalized Sinkhorn skill was only 0.00887 for cross-attention with PCA-cost OT, PCA-denoised velocity targets, and autonomous PCA-smoothed UCE input; its mean-shift R-squared remained negative at -0.0155. The target-only counterpart was nearly identical (skill 0.00864, R-squared -0.0178). Skip-1 reached 0.00835/-0.0174, while skip-2 was approximately persistence-level or worse. KNN, metacell, balanced, partial, and UOT alternatives did not establish meaningful positive dynamics prediction.
- Added a contiguous middle-three-stage forecast using `E13.5`, `E13.75`, and `E14.0` as one held-out block. Causal evaluation forecasts all three horizons from `E13.25`; training excludes every adjacent or skipped interval whose span touches the block. A fresh train-only PCA fit is job `19459275`. Dependent cross-attention PCA-denoised/PCA-UCE training jobs are adjacent `19459276`, skip-1 `19459278`, and skip-2 `19459280`, with causal evaluations `19459277`, `19459279`, and `19459281`.

## 2026-08-01

- First large-bank FiLM convergence results completed. Balanced/raw and partial/raw both stopped after 1,300 steps and selected step 100, with normalized validation EMA 1.00215 and 1.00560. PCA-cost/PCA-target denoising adjacent stopped after 3,500 steps and selected step 2,300 (1.03261); adding skip-1 stopped after 3,700 and selected step 2,500 (1.02070). Skip-1 is modestly better than adjacent-only under PCA denoising but remains worse than the normalized zero-velocity level. Final observed-interval rollout mean-shift R-squared remains negative for all four (-0.0296, -0.0358, -0.0277, and -0.0256, respectively). Their causal evaluations are eligible but waiting for GPU priority. No sweep jobs have failed.
- Added requested non-autonomous UCE-input tests for KNN and metacell smoothing. During fitting and fixed validation, UCE receives the interpolation between the same smoothed endpoints used to define the velocity target. During observed and held-out rollouts, UCE receives raw evolving expression with no smoothing. Evaluation outputs record both the `denoised` training preprocessing and `raw` rollout preprocessing. KNN/metacell smokes are `19414785`/`19414786`; dependent full FiLM jobs are `19414787`/`19414789`, and cross-attention jobs are `19414791`/`19414793`, with each following odd/even successor as its evaluation.
- Added a matched PCA-smoothed UCE conditioning arm. The autonomous field is `v(UCE(D(x)), CRE)`, where `D` is the train-fitted 100-PC gene-expression reconstruction. The same map is applied to raw interpolated expression during training and fixed validation, and reapplied to the current expression state at every observed or held-out Euler rollout step. The velocity target remains the PCA-reconstructed endpoint displacement. This isolates UCE input smoothing from the existing target-only PCA arm without conditioning on time or endpoint identity.
- All 40 tests pass, including NumPy/GPU PCA-reconstruction agreement. GPU smoke `19414538` gates FiLM training/evaluation `19414539`/`19414540` and cross-attention `19414541`/`19414542`. KNN and metacell UCE-input arms were not submitted because those operators require a timepoint-specific reference population and therefore do not define the same autonomous rollout contract.
- Added the two missing endpoint-denoising arms. KNN smoothing uses the 15 nearest cells in unwhitened train-fitted PCA coordinates within each sampled timepoint. Metacell smoothing clusters each 16,384-cell timepoint pool into 512 PCA metacells. Both average log-normalized gene profiles, restore each selected cell's library size, affect only gene-space velocity targets, and leave the raw expression interpolant as the online UCE state. Fixed validation applies the same method independently within validation cells at each observed stage.
- All 39 tests pass. KNN and metacell one-step GPU smokes are `19414372` and `19414373`. Full partial-OT/PCA-cost adjacent-interval jobs depend on both smokes: FiLM KNN/metacell `19414374`/`19414376` and cross-attention KNN/metacell `19414378`/`19414380`; each following odd job is its causal evaluation.
- Finalized the large-plan dynamics sweep at 16,384 cells per endpoint, 1,024 retained pairs per interval, and model microbatches of 8. Partial-OT pool benchmarks from 1,024 through 16,384 cells all completed. The 16,384-cell plan used 6.24 GiB peak GPU memory, took 2.07 seconds for transport after a 155.6-second atlas load, transported 0.94995 mass, and had approximately 4.43 million effective edges. Larger pools were not selected because repeated endpoint loading across 38 adjacent, 73 skip-1, or 105 skip-2 training intervals would dominate the 24-hour fitting budget.
- Replaced flattened coupling sampling with equivalent row-marginal then conditional-column sampling. This removes PyTorch's `2^24` multinomial-category limit, which was exposed by the first large-bank smoke. The corrected sampler passed a direct 16,785,409-edge regression test and the complete 37-test suite.
- Fit the train-only denoising artifact with 100 PCs over the 10,000-gene panel using 10,000 stage-balanced cells and excluding the within-stage validation split. The corrected skip-2 cross-attention smoke completed PCA-cost partial OT, PCA-reconstructed gene targets, raw-expression online UCE conditioning, and one optimizer step. Its 8,192-cell plan transported 0.94979 mass and had approximately 340,225 effective edges.
- Submitted the controlled 14-run training matrix with dependent causal evaluations. FiLM training jobs are `19413966`, `19413968`, `19413970`, `19413972`, `19413974`, `19413976`, and `19413978`; cross-attention jobs are `19413980`, `19413982`, `19413984`, `19413986`, `19413988`, `19413990`, and `19413992`. In each architecture the sequence is balanced/raw/adjacent, partial/raw/adjacent, UOT/raw/adjacent, partial/PCA-cost/adjacent, plus PCA-denoised targets, skip-1, and skip-2. The odd-numbered successor job after each training job is its causal evaluation.
- Implemented the next dynamics ablations. Interval-stratified OT banks build and retain a separate coupling sample for every training interval, shuffle model microbatches across intervals, and refresh each interval only after its bank is exhausted. Raw count interpolants remain the UCE state; optional train-only PCA reconstruction changes only gene-space velocity targets. Whitened train-only PCA coordinates can independently replace raw-expression cosine distance for OT.
- Added adjacent-plus-skip interval construction for skip-1 and skip-2. Any interval whose full temporal span contains a held-out stage is excluded, and early stopping remains based on adjacent observed intervals so validation scores are comparable across skip ablations. Added a standalone PCA fitting workflow and a GPU benchmark for 1,024-8,192-cell OT pools. The superseded sequential-pair sweep was cancelled after preserving its partial checkpoints and metrics.
- Added configurable coupling backends for balanced Sinkhorn OT, fixed-mass partial OT through dummy source/target points, and KL-marginal-relaxed unbalanced Sinkhorn OT. Coupling parameters are persisted in checkpoints, and training metrics now record plan mass, mean paired cost, effective edge count, maximum edge mass, and source/target marginal coefficient of variation.
- Decoupled OT population coverage from model activation size. A configurable large endpoint pool defines each transport plan; multiple endpoint pairs are sampled from that plan and consumed by small STREAM/UCE microbatches. The production sweep uses 1,024 cells per endpoint, 64 sampled pairs per plan, and model microbatches of 8. Fixed validation batches likewise draw eight pairs from deterministic 1,024-by-1,024 plans. This avoids the forced-match and poor cell-type-coverage problem of the previous 8-by-8 OT solve without increasing model memory; at 95% transported mass, partial OT may leave approximately 51 cells unmatched per endpoint pool.
- GPU smokes passed for partial OT with cross-attention and KL-UOT with FiLM at pool size 256, followed by a successful 1,024-cell partial-OT smoke. The 1,024 plan transported 0.94995 mass, had approximately 27,644 effective edges, and required well below available GPU memory. Submitted the full 12-way matrix of balanced/partial/UOT coupling, FiLM/cross-attention, and independent/TSS-context CRE representations; training jobs are `19413352` through `19413374` (even IDs) with dependent causal evaluations `19413353` through `19413375` (odd IDs).
- Both validation-selected reruns and their causal evaluations completed. Independent CRE stopped after 2,300 steps and selected step 1,100 (normalized validation EMA 1.00369); TSS-context stopped after 1,300 steps and selected step 100 (0.99978). Normalized validation MSE remained approximately 1.0, the zero-velocity reference value, and mean observed-interval rollout R-squared remained negative at every diagnostic check. Training longer therefore did not establish useful autonomous dynamics even on observed intervals.
- At 16 causal steps, independent-CRE Sinkhorn skill was 0.00577 into E9.5 and 0.00283 into E10.5, with mean-shift R-squared -0.0293 and -0.0330. TSS-context skill was 0.00488 and 0.00150, with R-squared -0.0467 and -0.0354. Both are effectively persistence-level; TSS-context again provides no improvement over independent CRE. Euler results were numerically unchanged at 4, 8, and 16 steps.

## 2026-07-31

- Replaced fixed-budget-only CFM fitting with validation-based checkpoint selection and early stopping. Ten percent of cells are held out independently within every observed training stage; fixed OT pairs and UCE encodings from those cells define an interval-balanced validation set. Selection uses an EMA of per-gene velocity-RMS-normalized CFM MSE, with a 0.5% minimum relative improvement and 12-check patience. Raw validation MSE and four-step autonomous mean-shift R-squared/MAE/Wasserstein diagnostics on observed intervals are logged separately. Held-out stages remain inaccessible during training and model selection. Best checkpoints now include optimizer state and stopping metadata for exact continuation.
- The early-stopping GPU smoke completed and wrote a reloadable model/optimizer checkpoint plus validation metrics. Submitted matched 10k online-UCE reruns for independent-CRE tokens (training `19412555`, causal evaluation `19412556`) and TSS-context tokens (training `19412557`, causal evaluation `19412558`). Both use a 50-epoch maximum; stopping is determined by the shared validation rule rather than that cap unless the validation score continues improving.

## 2026-07-29

- Expanded `docs/main.tex` from a methods draft into a paper-style manuscript with Abstract, Introduction, Results, Methods, Discussion, figures, data-source table, result tables, and inline references.
- Added reproducible manuscript result panels under `docs/figures/` and a `scripts/build_paper_figures.py` helper that reuses the slide-deck result summaries. The manuscript reports mouse 5k/10k held-out results, legacy-panel fair comparison, and zebrafish learned transfer regimes without showing the cross-species no-model comparator.
- Methods now explicitly document data provenance: mouse JAX/Shendure time-lapse expression data with ENCODE/SCREEN mm10 cCREs, and ZSCAPE control zebrafish data with ZEPA GRCz11 cCREs. The text clarifies that UCE is used for state conditioning while the predicted velocities and losses remain expression-valued.
- Added infrastructure for a mouse held-out-gene dynamics prediction test. The benchmark keeps OT, interpolation, cell state, and model outputs on the full 10k mouse gene panel, but restricts the training loss to a reproducible 8k-gene subset and evaluates velocity prediction on the 2k held-out genes. This tests whether CRE-conditioned shared gene weights generalize to genes not used in the loss; expression-only CFM is not a clean baseline because untrained output coordinates have gene-specific heads.
- Submitted the 10k UCE cross-attention held-out-gene mouse benchmark. Gene split: `outputs/stream_hvg10000/gene_holdout_seed1337_frac20_{train,heldout}.csv`. Train job `19392758`; dependent evaluation job `19392764` reports full, train-gene, and held-out-gene metrics.
- The mouse held-out-gene benchmark completed successfully. Across 20 held-out-timepoint evaluation batches, velocity MAE was 1.7334 on the 8,000 genes included in the loss and 1.7884 on the 2,000 excluded genes, a 3.2% held-out-gene gap. Full-panel velocity MAE was 1.7444. Displacement MAE showed the same 3.2% gap (0.4334 versus 0.4471).
- The held-out-gene velocity MAE was higher for every time interval: E9.25-E9.5, 1.7494 versus 1.7069; E9.5-E9.75, 1.9313 versus 1.8698; E10.25-E10.5, 1.6558 versus 1.6123; and E10.5-E10.75, 1.7610 versus 1.6880. Absolute loss and MSE should not be compared directly between gene subsets because their target-expression scale distributions differ.
- Added displacement R-squared relative to the mean target displacement within each evaluation batch. The rerun yielded mean R-squared of -0.0101 on training genes, -0.0118 on held-out genes, and -0.0104 on the full panel; only 2 of 20 batches were positive. Thus held-out genes perform similarly to fitted genes, but the model explains essentially none of the exact OT-paired displacement variance. This pairwise metric includes irreducible minibatch-OT coupling variation and should be complemented by a distribution-level transported-endpoint metric.
- Identified that cached-UCE training interpolated endpoint UCE embeddings, so its held-out evaluation was conditional on the destination state and did not define a causal expression rollout. Added an online-UCE contract in which raw-count OT interpolants are encoded directly and STREAM represents the autonomous field `dx/dt = v(UCE(x), CRE)`, with no explicit time input.
- Added causal evaluation restricted to observed-to-held-out intervals. It recomputes UCE after each projected Euler count-space step, fits a train-only 50-component PCA for endpoint scoring, and reports debiased Sinkhorn divergence, persistence-normalized Sinkhorn skill, gene-level mean-shift R-squared, and per-gene Wasserstein distance.
- CPU tests passed (23 tests). GPU smoke training passed at the intended 10k batch/chunk settings, and an end-to-end causal-evaluation smoke test produced both incoming held-out intervals without accessing endpoint UCE. Submitted corrected 10k held-out-gene training job `19404712` and dependent causal evaluation job `19404713`.
- Corrected online-UCE training and causal evaluation completed. The 4/8/16-step rollouts converged closely. At 16 steps, Sinkhorn skill relative to persistence was 0.0147 into E9.5 and -0.0268 into E10.5; full-panel mean-shift R-squared was -0.0012 and -0.0095. Held-out-gene mean-shift R-squared was similarly near zero (-0.0020 and -0.0101), so the causal model does not explain meaningful endpoint dynamics despite comparable fitted- and held-out-gene behavior.
- Added a contextual AlphaGenome CRE strategy. Each gene's TSS +/-100 kb locus is covered by two overlapping native 131,072 bp AlphaGenome inputs, both containing the TSS. Each CRE token is the mean 128 bp trunk representation over bins overlapping that CRE, taken from the window that places it farthest from an input edge. This contrasts with independently centering and globally pooling one AlphaGenome input per CRE.
- Contextual extraction geometry and causal token selection are covered by 25 passing tests. A two-gene GPU smoke job produced the expected `(2, 32, 3072)` token tensor. Submitted the full matched chain: contextual embedding `19408346`, online-UCE held-out-gene training `19408347`, and 16-step causal evaluation `19408348`.
- The TSS-context chain completed successfully but did not improve causal dynamics prediction. Relative to independent-CRE embeddings at 16 Euler steps, E9.25-E9.5 Sinkhorn skill decreased from 0.01475 to 0.01191 and full-panel mean-shift R-squared decreased from -0.00124 to -0.02549. E10.25-E10.5 Sinkhorn skill was effectively unchanged (-0.02677 versus -0.02626); mean-shift R-squared improved from -0.00946 to -0.00194, but mean-shift MAE and per-gene Wasserstein distance worsened for both intervals. The contextual strategy therefore provides no consistent benefit and remains at persistence-level endpoint performance.

## 2026-07-28

- Added a reproducible slide deck summarizing STREAM: multi-timepoint CFM, CRE-conditioned gene velocity prediction, UCE-state conditioning, mouse panel-size and UCE ablations, zebrafish benchmark setup, and cross-species transfer results.
- The deck emphasizes diagrams, existing atlas figures, result plots, and summary tables. It reports that 10k UCE cross-attention is the best mouse model and focuses the zebrafish transfer comparison on learned zero-shot, fine-tuned, and zebrafish-only regimes.
- Added an illustrative velocity-stream slide and a references slide covering Flow Matching, UCE, AlphaGenome, and the ZSCAPE atlas.

## 2026-07-13

- Checked the larger mouse STREAM panel results. The selected panels contain exactly 5,000 and 10,000 genes, and the legacy comparison panel contains 1,984 genes.
- Expression-state held-out full-panel mean losses: 5k standard CFM 41.6054, FiLM 44.1599, cross-attention 45.4073; 10k standard CFM 21.1570, FiLM 23.6008, cross-attention 20.8592. On the full panel, moving from 5k to 10k improves all model families, with 10k cross-attention slightly best by loss and 10k standard CFM slightly best by MAE.
- Legacy-panel fair comparison against the original 1,984-gene models: standard CFM improves slightly from 83.8798 to 82.8475 at 10k, cross-attention improves from 84.7884 to 80.5361 at 10k, and FiLM worsens from 82.7323 to 92.6408 at 10k. The 5k models do not improve the legacy-panel metric.
- UCE-state results: 10k UCE cross-attention is the best full-panel result, with mean loss 20.2780 and MAE 1.5938. On the legacy panel, 10k UCE cross-attention is also best among the larger-panel runs, with mean loss 78.7470 and MAE 4.1303.

## 2026-07-12

- Added the ZSCAPE zebrafish transfer workflow. It downloads public reference counts and metadata, GRCz11 genome/annotation, and ZEPA developmental cCRE workbooks; writes filtered control AnnData shards; caches zebrafish UCE embeddings; and links the primary cCRE catalogue to protein-coding HVGs with explicit promoter tokens. The dynamic supplement is included only when it exposes readable coordinate worksheets.
- Generalized STREAM time handling from implicit mouse day parsing to persisted physical-day or relative `[0,1]` coordinates. The same coordinate contract is used by training and held-out evaluation.
- Added source-checkpoint initialization, labeled artifacts, deterministic shared endpoint caches, and held-out expression-displacement MSE/MAE. This displacement metric is used across time scales because native velocity targets change with the coordinate transform.
- Added dependency submission for 5k/10k FiLM, cross-attention, and standard-CFM zebrafish comparisons under relative-time and physical-day conditions. Zero-shot uses the matching mouse source checkpoint, fine-tuning updates the STREAM field only, and UCE/AlphaGenome remain frozen.
- ZSCAPE conversion retained 630,179 uninjected 28C control cells in seven AnnData shards and produced 959,040 primary ZEPA cCREs. The dynamic workbook has no readable sheets and is skipped. Made streaming HVG selection independent of the Scanpy-heavy EDA module so it runs in the UCE training environment.
- Reduced the active zebrafish experiment to the best mouse architecture: 10k genes, frozen UCE cell state, and cross-attention STREAM. The retained comparisons are zero-shot transfer, fine-tuning, and zebrafish-only training under both physical-day and relative-time coordinates; 5k, FiLM, and standard-CFM jobs were cancelled.
- The initial zebrafish AlphaGenome CRE jobs exhausted memory while constructing a wide pandas table of Python float objects. CRE embeddings now stream into a resumable float32 matrix and token arrays are packed directly from that matrix; the target chains were resubmitted with zero-shot evaluation correctly waiting for target CRE tokens.
- The initial ZSCAPE conversion was restricted to 630,179 `ctrl-uninj` cells. Corrected the active filter to retain all 1,232,014 `ctrl-*` control cells, matching the public reference-atlas scale; cells without a stage label remain stored but are excluded from time-interval sampling.

## 2026-07-06

- Added larger STREAM panel support for 5k and 10k protein-coding HVG runs.
- Existing `streaming_hvg_genes.csv` only contains 2,000 genes, so larger runs need a fresh streaming gene-variance table. Added `scripts/select_streaming_hvgs.py` and `slurm/run_stream_select_hvgs.sbatch` to compute this without rerunning the full UMAP workflow.
- STREAM scripts now accept `--hvg-csv`, `--n-hvg`, and `--out-dir` overrides, with matching Slurm environment variables. This keeps 5k/10k checkpoints and metrics in separate output directories instead of overwriting `outputs/stream/`.
- Gene selection now sorts by variance before taking protein-coding HVGs and selects a buffer before GTF matching, then trims after TSS annotation. This preserves the legacy <2k panel behavior when only the 2,000-row HVG file is available, while allowing exact larger panels when the full variance table is present.
- Held-out evaluation now reports `eval_gene_set`, `n_eval_genes`, full-panel metrics, and optional subset-panel metrics from a reference `selected_genes.csv`. This supports scoring 5k/10k models on both their full modeled gene sets and the legacy `outputs/stream/selected_genes.csv` panel.
- Compute-node unit test job `18708907` passed: 4 tests passed, 5 torch-dependent tests skipped because the cluster PyTorch module could not import torch without `libibverbs.so.1`.
- GPU check job `18708941` confirmed `~/venv/torchfix/bin/python` can import torch 2.11.0+cu128 and see an NVIDIA L40S. The GPU STREAM Slurm scripts now default to this venv via `STREAM_PYTHON`, with the PyTorch module retained only as a fallback.
- GPU smoke test job `18708953` exercised `evaluate_intervals` with full and legacy gene-set metrics on CUDA and passed.
- Submitted 5k/10k rerun dependency chain. Streaming variance job: `18709007`. 5k jobs: prepare `18709008`, embed `18709009`, train/eval standard CFM `18709010`/`18709011`, train/eval FiLM `18709012`/`18709013`, train/eval cross-attention `18709014`/`18709015`. 10k jobs: prepare `18709016`, embed `18709017`, train/eval standard CFM `18709018`/`18709019`, train/eval FiLM `18709020`/`18709021`, train/eval cross-attention `18709022`/`18709023`.

## 2026-07-07

- Checked the larger-panel Slurm chain. The HVG, prepare, CRE embedding, and standard CFM training jobs completed for both 5k and 10k panels. The 5k standard CFM eval also completed; 10k standard CFM eval `18709019` is still pending on GPU priority.
- FiLM and cross-attention STREAM training failed quickly for both 5k and 10k panels with CUDA OOM. The failure occurred when the model expanded regulatory token states to `[batch, genes, tokens, hidden]` for all genes at once.
- Canceled stale dependent eval jobs `18709013`, `18709015`, `18709021`, and `18709023`, which were pending with `DependencyNeverSatisfied`.
- Added gene-chunked STREAM prediction and loss computation. STREAM training and evaluation now process genes in configurable chunks (`gene_chunk_size`, default 512), preserving the same full-panel MSE objective while avoiding the full batch-by-gene activation tensor.
- Submitted guarded STREAM restart chain with `BATCH_SIZE=32` and `GENE_CHUNK_SIZE=512`. Smoke jobs: FiLM `18712595`, cross-attention `18712596`. Full jobs depend on those smoke jobs: 5k FiLM train/eval `18712617`/`18712618`, 5k cross-attention train/eval `18712619`/`18712620`, 10k FiLM train/eval `18712621`/`18712622`, and 10k cross-attention train/eval `18712623`/`18712624`.

## 2026-07-08

- Updated STREAM architecture so both conditioning mechanisms are applied at every transformer layer. FiLM now uses layer-specific cell-state MLPs to produce scale/shift vectors after each CRE self-attention block. Cross-attention now uses layer-specific cell-state context-token MLPs and cross-attention modules after each CRE self-attention block.
- Updated chunked STREAM training to backpropagate each gene chunk immediately. This avoids retaining autograd graphs for every gene chunk before a single backward pass.
- Canceled the pending pre-layerwise queue chain and resubmitted jobs against commit `d6e4516`. Fresh jobs: 10k standard CFM eval `18730278`; smoke jobs FiLM `18730279` and cross-attention `18730280`; 5k FiLM train/eval `18730281`/`18730282`; 5k cross-attention train/eval `18730283`/`18730284`; 10k FiLM train/eval `18730285`/`18730286`; 10k cross-attention train/eval `18730287`/`18730288`.

## 2026-07-09

- Checked layerwise job status. The 10k standard CFM eval `18730278` completed and wrote `outputs/stream_hvg10000/eval_metrics_standard_cfm.csv`. The 5k layerwise smoke jobs `18730279` and `18730280` completed, but full layerwise STREAM jobs with `BATCH_SIZE=32` and `GENE_CHUNK_SIZE=512` failed from CUDA OOM; dependent evals were left with `DependencyNeverSatisfied`.
- Canceled the stuck dependent evals `18730282`, `18730284`, `18730286`, and `18730288`. Resubmitted lower-memory layerwise STREAM jobs using `BATCH_SIZE=8` and `GENE_CHUNK_SIZE=256`: 5k FiLM train/eval `18755345`/`18755346`, 5k cross-attention train/eval `18755347`/`18755348`, 10k FiLM smoke/train/eval `18755349`/`18755351`/`18755352`, and 10k cross-attention smoke/train/eval `18755350`/`18755353`/`18755354`.

## 2026-07-10

- Low-memory layerwise STREAM jobs completed successfully. Completed jobs: 5k FiLM train/eval `18755345`/`18755346`, 5k cross-attention train/eval `18755347`/`18755348`, 10k FiLM smoke/train/eval `18755349`/`18755351`/`18755352`, and 10k cross-attention smoke/train/eval `18755350`/`18755353`/`18755354`.
- Mean held-out full-panel losses: 5k standard CFM 41.6054, 5k FiLM 44.1599, 5k cross-attention 45.4073; 10k standard CFM 21.1570, 10k FiLM 23.6008, 10k cross-attention 20.8592. On mean MAE, standard CFM remained best for 5k and 10k full-panel evaluation.
- Benchmarked UCE as a replacement cell-state representation. The official 33-layer checkpoint loaded strictly into the reference architecture (1,280 hidden dimensions, 20 attention heads, 5,120-dimensional feed-forward layers). UCE samples 1,024 genes per cell. Atlas-derived tokenization produced only 1,063- and 1,065-token cells, so batches can be bucketed by exact length. At 1,065 tokens, checkpoint-backed BF16 inference with the reference padding mask sustained 116.8 cells/s (27.2 GPU-hours for all 11,441,407 cells). Bucketing and omitting the padding mask for exact-length batches selected PyTorch Flash Attention, sustained 131.2 cells/s, and projects to 24.2 GPU-hours on one GPU. The masked and mask-free BF16 embeddings had mean cosine similarity 0.999972. Batch sizes 50 and 100 had equivalent throughput; batch 50 is the practical initial setting.
- Sparse backed AnnData reads and UCE-style weighted gene sampling were measured at 487 cells/s (about 6.5 CPU-hours for the atlas), so they can be pipelined with GPU inference. The reference UCE preprocessor is unsuitable as-is because it materializes a dense `int64` cell-by-gene memmap; production embedding must stream sparse rows and write the 1,280-dimensional output incrementally. The final output alone is about 54.6 GiB as float32.
- Added the UCE-state STREAM ablation. The streaming UCE job creates one float16 embedding matrix per AnnData file and uses BF16 plus exact-length buckets (1,063 or 1,065 tokens) to select Flash Attention. CPU tokenization of the next sparse chunk runs while the GPU embeds the current chunk. In UCE-state training, OT, interpolation, target velocity, and loss remain in expression space; UCE endpoint embeddings are interpolated using the sampled OT pairs and CFM time, then supplied as the vector-field input. UCE checkpoints and metrics use a `_uce` suffix so expression-state results are preserved.
- Submitted UCE cache job `18756697` with a 30-hour limit. The dependency chain contains 5k standard CFM train/eval `18756698`/`18756699`, 5k FiLM `18756700`/`18756701`, 5k cross-attention `18756702`/`18756703`, 10k standard CFM `18756704`/`18756705`, 10k FiLM `18756706`/`18756707`, and 10k cross-attention `18756708`/`18756709`. Training uses online Weights & Biases logging and all evaluations include the common legacy 1,984-gene panel.
- UCE cache job `18756697` completed in 24:36 and produced all four embedding matrices. Its initial training chain failed before Python startup because the selected Python binary required a newer GLIBC than the L40S nodes provide. A L40S runtime smoke job `18775476` passed with the compatible PyTorch environment. Canceled the stale dependent evaluations and resubmitted against the completed cache: 5k standard CFM train/eval `18775478`/`18775479`, 5k FiLM `18775480`/`18775481`, 5k cross-attention `18775482`/`18775483`, 10k standard CFM `18775484`/`18775485`, 10k FiLM `18775486`/`18775487`, and 10k cross-attention `18775488`/`18775489`.
- All resubmitted UCE-state jobs completed. On the 5k full panel, UCE improved loss for FiLM (41.7723 versus 44.1599) and cross-attention (43.7655 versus 45.4073), while standard CFM was slightly worse (41.9452 versus 41.6054). On the 10k full panel, UCE FiLM improved to loss/MAE 22.0919/1.6288 from 23.6008/1.6688, and UCE cross-attention improved to 20.2780/1.5938 from 20.8592/1.7424. UCE cross-attention is the best 10k result on both metrics, slightly ahead of expression-state standard CFM (21.1570/1.6024). The common legacy 1,984-gene panel shows the same 10k FiLM/cross-attention improvements; UCE standard CFM worsened on both panel sizes.
- All corrected UCE-state runs completed. At 10k genes, UCE FiLM improved full-panel held-out loss/MAE from 23.6008/1.6688 to 22.0920/1.6288; UCE cross-attention improved from 20.8592/1.7424 to 20.2780/1.5938. The 5k UCE FiLM and cross-attention variants improved loss but not MAE; the UCE standard CFM baseline was worse than its expression-state counterpart at both panel sizes.
- Added a zero-velocity persistence baseline for held-out timepoint evaluation. It has no learned parameters and predicts no expression displacement over each OT-matched interval, using the same cached endpoint batches as learned models.
- Device-matched zebrafish persistence evaluations (CUDA, 10k genes) achieved displacement MAE 0.11036 and MSE 3.32469 for both relative and physical-day coordinates. This outperformed all completed learned transfer variants on both metrics, indicating that these models have not yet exceeded a no-change predictor on the held-out endpoint task.
